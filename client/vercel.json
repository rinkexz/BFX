{
    "projects": {
      "default": "client"
    },
    "build": {
      "installCommand": "npm install --force",
      "buildCommand": "npm run build",
      "outputDirectory": "client/.next"
    }
  }